# Control de Tareas - CUDII

## Fase 1
- [x] Fase 1 Completada y Validada (Infraestructura, Auth, Base de Datos Multitenant)

## Tareas Pendientes / Mejoras Futuras (Backlog)
- [ ] **Polishing Onboarding & Login:** Conectar enlace de registro desde el Login, probar flujo completo de Onboarding desde el navegador y normalizar diseño visual (UI/UX).
- [x] **Historial de Mi Perfil:** Extender la vista de `/admin/perfil` para mostrar un log/histórico de la actividad del usuario (inicios de sesión, ventas, devoluciones, cortes, etc.). Backend: `GET /audit/me` (cualquier usuario autenticado ve sus propios logs) y registro de `INICIO_SESION` en `auth.service.ts`. Frontend: sección "Historial de Actividad" en `MiPerfilView.tsx` con etiquetas legibles y severidad.
- [x] **Campana de Notificaciones en Frontend:** `NotificationBell.tsx` en `MainLayout` conectada al backend (`GET /notifications`, `GET /notifications/count`, `PATCH /notifications/:id/read`, `PATCH /notifications/read-all`). Contador de no leídas con polling 60s, dropdown/popover con lista paginada, filtro No leídas/Todas, marcar como leída al abrir, "marcar todas como leídas" y estilos Whitelabel claro/oscuro con la tipografía del sitio (font-headline-md / body-md / label-sm).

## Fase 2: Catálogo y Gestión de Inventario
- [x] B1 — Extender Schema Prisma (Categoria, HistorialPrecioProducto)
- [x] B2 — Módulo categories (Backend CRUD)
- [x] B3 — Módulo products (Backend CRUD con historial)
- [x] B4 — Módulo inventory (Backend Ajustes/Movimientos)
- [x] B5 — Módulo users (Backend CRUD y Roles)
- [x] B6 — Panel de Administración (Frontend: Rutas y Vistas)
- [x] **Validación Multi-Sucursal:** Verificado que los modales de Productos, Inventario y Usuarios funcionen aislados por sucursal (`empresaId` / `sucursalId`).

## Fase 3: Terminal POS y Ciclo de Venta Completo
- [x] C1 — Extender Schema Prisma (`SesionCaja`, `Venta`, `DetalleVenta`, `PagoVenta`, `RetiroParcial`, `CorteX`, `CorteZ`, `Devolucion`, `DevolucionProducto`). Migración dev aplicada.
- [x] C2 — Módulo backend `cash-register` (apertura, retiros parciales, Corte X, Corte Z configurable ciego/abierto).
- [x] C3 — Módulo backend `sales` (registro atómico de venta en `prisma.$transaction`, generación de folio por caja `CJ01-000001`, ajuste de inventario permisivo auditado).
- [x] C4 — Módulo backend `returns` (devoluciones parciales/totales con separación stock vs merma).
- [x] C5 — Terminal POS en Frontend (`PosView.tsx`, `ProductSearch`, `ProductGrid`, `CartItem`, `CartSummary`, `CheckoutModal`, `VoucherModal`, `OpenCashRegisterModal`, `CloseRegisterModal`, `CashWithdrawalModal`). Persistencia del carrito en `sessionStorage`.
- [x] C6 — Pantalla de Devoluciones en Frontend (`DevolucionesView.tsx` en `/admin/devoluciones`).
- [x] C7 — Rediseño estético POS bento visual sin imágenes según `code.html`, con soporte Whitelabel claro/oscuro, cliente API unificado y marcación rápida por categorías.
- [x] **Configuración del Sitio (menú de perfil):** Nuevo módulo backend `company-settings` (`GET/PATCH /company-settings`) con RBAC ADMIN/SUPER_ADMIN, nueva vista `ConfiguracionView.tsx` en `/admin/configuracion` para editar el tipo de Corte Z (ciego/abierto) a nivel empresa, item "Configuración del Sitio" en el dropdown de perfil, y modal de Corte Z que muestra el efectivo esperado en modo abierto. Verificado por API (RBAC, validación, ambos modos de corte).

### Validación Fase 3 (cortes + auditoría + notificaciones)
- [x] **Migración `add_audit_notifications_umbral` aplicada** a BD: tablas `LogActividad` y `Notificacion`, `Empresa.umbralFaltanteCritico` (default 50), `CorteZ.tipoDiscrepancia` y `CorteZ.autorizadoPorId`.
- [x] **Corte Z con discrepancias en backend:** `cuadre` (info) / `sobrante` (warning, notas obligatorias) / `faltante` (warning o `critical` si supera el umbral; requiere `autorizadoPorId` de SUPER_ADMIN/ADMIN/GERENTE). Notificaciones a admins/gerentes (`caja_abierta`, `retiro_parcial`, `corte_z_realizado`, `faltante_caja`). Verificado 14/14 por API.
- [x] **Auditoría registrada en `LogActividad`:** `APERTURA_CAJA`, `RETIRO_PARCIAL`, `CORTE_X`, `CORTE_Z` con severidad y payload completo.
- [x] **Fix compilación backend:** `CrearLogDto.detalles` ahora es `Prisma.InputJsonValue` (antes `Record<string, unknown>` impedía compilar y el watcher corría un build viejo).
- [x] **Umbral de faltante configurable:** `company-settings` expone/edita `umbralFaltanteCritico`; `ConfiguracionView` permite ajustarlo (input numérico).
- [x] **Endpoints para el flujo POS:** `GET /cash-register/settings` (modoCorteZ + umbral, roles ADMIN/GERENTE/CAJERO) y `GET /users/authorizers` (lista de autorizadores de la empresa).
- [x] **Histórico de actividad:** `AuditController` (`GET /audit` con filtros accion/severidad/entidadTipo/fechas + paginación, RBAC SUPER_ADMIN/ADMIN/GERENTE) y vista `AuditoriaView.tsx` en `/admin/auditoria` con filtros y tabla paginada.
- [x] **Corte Z en frontend:** `CloseRegisterModal` con diferencia en vivo (cuadre/sobrante/faltante), notas obligatorias cuando hay discrepancia, selector de autorizador para faltantes críticos y pantalla de resumen del resultado.

- [x] **Mejora del Dashboard Personalizable:** Solucionado bloqueo estricto de arrastre cuando la edición está desactivada (`static: !personalizando`), habilitado redimensionamiento interactivo de paneles con `WidthProvider(Responsive)`, implementado layout canónico por defecto con botón "↺ Restaurar predeterminados" y acotamiento de estilos responsivos aislados en `.dashboard-view-container`.

## Fase 4: Fidelización, Crédito y Panel de Control
- [x] D1 — Modelo de datos de clientes: `Cliente`, `CuentaCreditoCliente`, `AbonoCredito`, `NivelLealtad`, `MovimientoPuntos` (migración `20260820000003_add_clientes_credito_lealtad`).
- [x] D2 — Programa de lealtad configurable (migración `20260820000004_add_programa_lealtad_configurable`) + `GET/PATCH /company-settings/lealtad` por rol.
- [x] D3 — Módulo backend `customers`: CRUD, cuenta de crédito/fiado, registrar abono (`PaymentDto`: `monto`+`metodoPago`, opcionales `referencia`/`notas`), saldo pendiente.
- [x] D4 — Módulo backend de clientes en ventas: venta al crédito, cobro de deuda en `CheckoutModal` (referencia "Venta $X · Deuda $Y", sin doble conteo), indicador cliente/público, `VentaDetalleView` con sección de venta a crédito.
- [x] D5 — Canje de puntos FIFO + redondeo r2 + desglose de descuentos en ticket.
- [x] D6 — Dashboard personalizable con react-grid-layout: KPIs, tendencia, métodos de pago, TOP y mini-TODO; rejilla arrastrable/redimensionable solo en modo edición; layout persistido (`preferenciasDashboard.layout`) con auto-save; `generarLayoutDefecto` estable + "↺ Restaurar predeterminados".
- [x] D7 — Modal de detalle de clientes reestructurado (scroll interno, layout fijo) + tooltip de vencimiento + caducidad editable.
- [x] **Migración `20260820000005_add_timestamps_audit` aplicada:** añade `creadoEn`/`actualizadoEn` a 14 tablas (Sucursal, Caja, InventarioSucursal, DetalleVenta, PagoVenta, DetalleVentaLote, SesionCaja, CorteZ, Devolucion, DevolucionProducto, NivelLealtad, MovimientoPuntos, CuentaCreditoCliente, AbonoCredito) para auditoría. Verificado en BD (14/14 columnas presentes).
- [x] **Refactor backend controladores `@Request()` → `@CurrentUser()`:** sales, returns, merma, cash-register (6 métodos) y notifications (4 métodos) con `import type { CurrentUserPayload }` (compatible con `isolatedModules`/`emitDecoratorMetadata`).
- [x] **Fix `suppliers.service.ts`:** `any` → `Prisma.ProveedorWhereInput`.
- [x] **Verificación Fase 4 final:** `tsc --noEmit` limpio en backend y frontend; endpoints clave (dashboard, products, categories, lealtad, customers, audit, suppliers, notifications, sucursales, reports, sales) → HTTP 200 con token real; dashboard PATCH/GET persistencia de `layout` confirmado.

### Validación de seguridad (revisión Fase 4)
- [x] `.env` en `.gitignore` y **no rastreado**; solo `.env.example` (plantilla) versionado — confirmado con `git ls-files`.
- [ ] **Pendiente (menor):** habilitar `strict: true` en `frontend/tsconfig.app.json`.
- [ ] **Pendiente (menor):** mover `@types/react-grid-layout` de `dependencies` a `devDependencies` (es solo de tipos).

### Correcciones de sesión (Fase 4 - post-revisión)
- [x] **#1 Sidebar por rol:** creado `frontend/src/lib/permisos.ts` (mapa central `ROLES_POR_MENU` + helper `puedeVerMenu`; SUPER_ADMIN ve todo); `MainLayout.tsx` filtra secciones/ítems por `user.rol`. `App.tsx` añade `RequireRol` y envuelve las rutas restringidas para alinearse con el backend.
- [x] **Fix página en blanco (runtime):** `MainLayout.tsx` importaba `ClaveMenu` (un tipo TS) junto al valor `puedeVerMenu` en la misma sentencia `import`. Vite/esbuild eliminaba el tipo → `SyntaxError: does not provide an export named 'ClaveMenu'`, rompiendo el montaje de React (todo en blanco). Fix: separar con `import { puedeVerMenu }` + `import type { ClaveMenu }`. Verificado con Chrome headless (el Dashboard renderiza sin errores de consola).
- [x] **#2 Reportes (Error 500) arreglado:** causa raíz en `reports.service.ts` `construirWhereSqlVenta` — `Prisma.join(condiciones)` usaba separador `,` por defecto (error SQL) al pasar `fechaInicio`/`fechaFin`. Fix: `Prisma.join(condiciones, ' ')` + manejo de array vacío con `Prisma.empty`. Verificado: `top-products` y `margin` (por producto/categoría) con rango → HTTP 200.
- [x] **#3 Auditoría de eventos sensibles:** inyectadas llamadas de `AuditService.registrarEvento` en `sales.service.ts` (`VENTA_COMPLETADA` con folio/total/subtotal/impuestos/descuento/método de pago/esDemostración, posterior a la transacción) y en `returns.service.ts` (`DEVOLUCION_REGISTRADA`). `AuditoriaView.tsx` añade ambas acciones a `ACCIONES`/`ACCION_LABEL` y casos legibles en `resumirDetalles`. Verificado end-to-end: crear venta real → log `VENTA_COMPLETADA` visible en `GET /audit` con filter.



## Fase 4.5: Cupones de Descuento
- [x] **Migración 20260828000000_add_cupones aplicada** a BD (workaround manual + migrate resolve --applied): tablas Cupon (16 columnas) y CuponRedencion (6 columnas), columna Venta.descuentoCupon (double default 0). migrate status = up to date.
- [x] **Módulo backend coupons:** CRUD multitenant (GET /coupons, GET /coupons/validar, GET /coupons/:id, POST /coupons, PATCH /coupons/:id, DELETE /coupons/:id) con RBAC (list/validar ADMIN,SUPER_ADMIN,GERENTE,CAJERO; crear/actualizar/detalle ADMIN,SUPER_ADMIN,GERENTE; delete ADMIN,SUPER_ADMIN). validar() chequea existencia, activo, vigencia, soloClientesRegistrados, monto mínimo de compra, límite total y límite por cliente; calcularDescuento() aplica MONTO_FIJO (min(valor, base)) y PORCENTAJE (0-100).
- [x] **Integración en ventas:** CreateVentaDto añade codigoCupon?; sales.service.createSale valida el cupón dentro de la transacción (base = subtotal - descuentos de ítems - descuento general), aplica el descuento en cascada (promo -> cupón -> nivel -> canje), guarda descuentoCupon y la CuponRedencion, y findOneSale incluye cuponRedencion.cupon.
- [x] **Frontend CuponesView.tsx** (/admin/cupones): CRUD completo con búsqueda, modal crear/editar (tipo %/$ , monto mínimo, switch solo clientes registrados, límites total/por cliente, fechas, activo) y columna de usos (_count.redenciones). Registrado en App.tsx, MainLayout.tsx (grupo Administración) y permisos.ts (clave cupones, ROLES ADMIN/GERENTE).
- [x] **Integración en cobro (frontend):** CheckoutModal añade input de código de cupón + botón Aplicar que valida vía GET /coupons/validar, calcula descuentoCupon en el desglose y envía codigoCupon en el payload; se re-valida al cambiar de cliente. VoucherModal y VentaDetalleView reflejan descuentoCupon y el código/cupón de cuponRedencion.
- [x] **Verificación end-to-end:** venta real con cupón PORCENTAJE (descuento 15%) y MONTO_FIJO () -> descuentoCupon correcto, CuponRedencion creada y findOneSale devuelve el cupón; límites total y por cliente rechazados con HTTP 400 y mensajes claros. tsc --noEmit limpio en frontend y backend.
- [x] **Fix re-validación del cupón al cambiar el carrito:** `CheckoutModal.tsx` ahora guarda `montoMinimoCompra` en `cuponAplicado`, y un `useEffect` observa la base activa del carrito (subtotal - descuentos de ítems - descuento general). Si un cupón aplicado deja de cumplir el monto mínimo (p. ej. el subtotal baja de $200 a $168), se quita automáticamente y se muestra la leyenda **"El cupón {codigo} no pudo aplicarse por ... requiere una compra mínima de $X"** (nuevo estado `cuponInvalido`, render en el bloque del cupón). También se limpia la leyenda al cambiar de cliente y al re-aplicar/quitar manualmente. Verificado por API (validar base 200 OK / base 168 HTTP 400) y commit `d3f5409`.

### Auditoría de tipografía (fuente CUDII: Geist + JetBrains Mono)
- [x] **Fuente establecida:** Sistema de diseño (POS/DASHBOARD/ONBOARDING `DESIGN.md`) define **Geist** para textos/títulos y **JetBrains Mono** para valores data-heavy (precios, SKU, folios, timestamps). Cargadas vía Google Fonts en `index.html` y mapeadas en `index.css` como tokens Tailwind (`font-body-md`, `font-headline-md`, `font-display-lg`, `font-body-lg` = Geist; `font-label-sm` = JetBrains Mono).
- [x] **Auditoría semántica pestaña a pestaña:** revisados todos los `.tsx`/`.ts`. Uso correcto de tokens CUDII: `font-label-sm` (176), `font-body-md` (42), `font-display-lg` (43), `font-headline-md` (46), `font-body-lg` (5). Sin `font-sans`/`font-serif`/familias inline extrañas (los `style={{...}}` solo usan color/width/transform, jamás `fontFamily`).
- [x] **Corrección:** la utilidad Tailwind `font-mono` (~107 usos en Reportes, Dashboard, Cajas, Devoluciones, POS, etc.) apuntaba a la pila mono genérica del sistema en lugar de JetBrains Mono. Fix en `index.css` (`@theme` redefine `--font-mono` → "JetBrains Mono", primera opción), alineando de forma global y automática todos esos valores con la fuente CUDII. Verificado que Tailwind v4 procesa el CSS con JetBrains Mono; commit `704c790`.

## Fase 4.6: Combos y Paquetes (Promociones, D11)
- [x] **Migración `20260829000000_add_combos` aplicada:** enum `TipoPrecioCombo` (MONTO_FIJO / DESCUENTO_PCT), tablas `Combo` (empresaId, nombre, descripcion, tipoPrecio, valorPrecio, activo, fechas, creadoPorId, timestamps + index por empresa/activo y fechas) y `ComboProducto` (unique comboId+productoId); `DetalleVenta` gana `comboId` + `nombreCombo` (snapshot) con `@@index([comboId])`.
- [x] **Módulo backend `combos`:** CRUD multitenant (GET /combos con paginación/búsqueda/vigentes, GET /combos/:id, POST, PATCH, DELETE soft) con RBAC (listar/consultar ADMIN/GERENTE/CAJERO; crear/editar/eliminar ADMIN/GERENTE). `calcularResumen` devuelve precioOriginal/precioCombo/ahorro desde `precioVentaBase` actual. Validaciones: ≥1 producto con cantidad > 0, sin duplicados, MONTO_FIJO con ahorro real (precio < Σ individual), DESCUENTO_PCT 1–90%, rango de fechas correcto. Registrado en `AppModule`.
- [x] **Verificación backend no-compila:** el build TS rompía por crash-loop (`rootDir`/`incremental`/`deleteOutDir`) y por `@prisma/client` desactualizado. Fix: `tsconfig.json`/`tsconfig.build.json`/`nest-cli.json` (rootDir: ./src, include src, incremental false, deleteOutDir false) + `npx prisma generate`. `tsc -p tsconfig.build.json` queda con 0 errores D11 (solo los 2 pre-existentes de `@nestjs/throttler`, módulo no instalado).
- [x] **Integración en `POST /sales`:** `CrearVentaDto` añade `combos?: [{comboId, cantidad}]` y `detalles` deja de exigir `ArrayMinSize(1)`; el servidor expande los combos en líneas internas con precios autoritativos de BD, descuenta inventario/lotes por producto y escribe `DetalleVenta.comboId`/`nombreCombo`. Venta solo-combio, mixta (suelto + combo) y por % vigente validadas; rechaza combo desactivado/inválido.
- [x] **Auditoría de combos:** `combos.service` registra `COMBO_CREADO`/`COMBO_ACTUALIZADO` (info) y `COMBO_DESACTIVADO` (warning) vía `AuditService` con `entidadTipo: 'combo'`, `entidadId` y detalles (nombre, tipoPrecio, valorPrecio, #productos, precios, ahorro, activo). `AuditoriaView` añade las 3 acciones a ACCIONES/ACCION_LABEL y `resumirDetalles`. Verificado end-to-end en `GET /audit`.
- [x] **Frontend POS:** `ComboCard`, `ComboDetalleModal` (desglose + cantidad), chip "Combos" en catálogo, auto-detección en carrito (al cubrirse un combo vigente se aplica automáticamente reemplazando las líneas sueltas, lógica en `PosView` + `aplicarComboSugerido`), y `CartComboItem`/`CartItem` con el combo como bloque en el ticket. `usePosStore` con addCombo/removeComboGrupo/detectarCombosDisponibles/applyCombo + cache.
- [x] **Frontend Admin:** `CombosView` (/admin/combos) estilo CuponesView con builder de items, búsqueda de productos, suma individual y ahorro en vivo. Registrado en `App.tsx` (ruta protegida ADMIN/GERENTE), `permisos.ts` (clave `combos`) y `MainLayout.tsx`.
- [x] **Detalle de venta con combo:** `findOneSale` incluye la relación `combo` y devuelve `comboId`/`nombreCombo`/`descuento` por línea; `VentaDetalleView` muestra badge "Combo: {nombre}" y columna de descuento por línea.
- [x] **Sin campos muertos:** tipos `Combo`/`ComboProducto` en `types/pos.ts` sin campos sin uso; eliminado estado muerto `selectedCajaId` del `usePosStore`; include de producto del combo recortado a lo consumido.
- [x] **Sidebar recomposado por secciones temáticas y verificado por rol:** cada ítem coincide con `permisos.ts`, el guard de ruta de `App.tsx` y los `@Roles()` del backend. Se movió **Cajas Abiertas** a la sección Operaciones y se creó la sección **Promociones** (Cupones + Combos).
- [x] **Fuentes verificadas en integraciones D11:** solo Geist / JetBrains Mono / Material Symbols Outlined en todo CUDII; combos y detalle reutilizan los tokens `font-body-md`/`font-label-sm`/`font-mono` sin fuentes externas ni valores arbitrarios.
- [x] **E2E `tests/run_tests.js` sección [13] (D11):** 14 casos (crear combos fijo/% con ahorro, rechazos de validación, ventas solo/mixto/%, detalle con comboId, desactivado). **Resultado global: 53 passed, 0 failed.**

## Fase 4.7: Presupuestos (Cotizaciones, D12)

- [x] **Migración `20260830000000_add_presupuestos`:** `Presupuesto` (empresaId, cajeroId, clienteId?, folio, secuenciaFolio, estado, subtotal/descuento/impuestos/total + descuentoNivel/descuentoCanje/descuentoCupon, snapshots codigoCupon/puntosACanjear/descuentoGeneral, timestamps, `@@unique([empresaId, folio])` + índices) y `PresupuestoDetalle` (productoId, nombreProducto, unidadMedida, cantidad, precioUnitario congelado, descuento, subtotal, total, comboId/nombreCombo); `Empresa` + `conservarPrecioPresupuesto` (default true) y `secuenciaPresupuesto` (default 0). Aplicada a BD (`applied = t`).
- [x] **Refactor `SalesService`:** helper compartido `sales/pricing.helper.ts` (`expandirCombos`/`LineaVenta`); `CrearVentaDto` + `presupuestoId?`; `ItemDetalleVentaDto` + `comboId?`/`nombreCombo?`; soporte de precios congelados cuando `conservarPrecioPresupuesto` está activo; marcado atómico `estado=vendido` en la transacción de la venta.
- [x] **Módulo backend `presupuestos`:** `POST /presupuestos` (crear snapshot sin caja/inventario, folio atómico `P-000001` vía `secuenciaPresupuesto`), `GET /presupuestos` (paginado/filtros), `GET /presupuestos/:id` (detalle con precioCongelado/precioActual/efectivo), `PATCH /presupuestos/:id/cancelar`. RBAC (crear/listar/detalle ADMIN/GERENTE/CAJERO; cancelar ADMIN/GERENTE) y registro en `AppModule`.
- [x] **Config empresa:** exponer `conservarPrecioPresupuesto` en `GET/PATCH /company-settings` y en `UpdateSettingsDto`.
- [x] **Frontend POS/store:** `usePosStore` con `presupuestoActivoId`, `setPresupuestoActivo` y `cargarPresupuesto` (`LineaPresupuesto`); `CartItem` + `comboId?`/`nombreCombo?`; botón "Guardar como presupuesto" en `CheckoutModal`; envío de `presupuestoId` en `/sales`.
- [x] **Frontend PresupuestosView:** `PresupuestosView.tsx` lista paginada (folio/cliente/estado/fecha/total) + Vender (carga al ticket → `/pos`), Ver detalle (modal con desglose), Cancelar. Ruta `/presupuestos`, `permisos.ts` (clave `presupuestos`, ADMIN/GERENTE/CAJERO), `MainLayout` (sección Operaciones).
- [x] **ConfiguracionView:** sección "Presupuestos" con `<Switch>` `conservarPrecioPresupuesto`.
- [x] **E2E `tests/run_tests.js` sección [14] (D12):** 20 casos (crear con producto y con combo, folio P-, rechazo vacío, listar paginado, detalle con precios congelado/actual/efectivo, venta por `presupuestoId`, rechazo de doble venta, cancelar, rechazo de cancelar vendido). **Resultado global: 73 passed, 0 failed (53 previos + 20 D12).** `tsc --noEmit` limpio en backend y frontend.
- [x] **UX detalle/venta presupuesto (D12):** rediseño del modal de detalle (encabezado fijo con ícono/título/estado, folio/fecha, tarjeta de metadatos, desglose de líneas, total a cobrar, botón fijo inferior), corrección de la X superior que chocaba con el badge de estado, y expansión de combos en el ticket (grupo expandible con cabecera y productos individuales) + indicador de cambio de precio por línea (`cotizado → actual · se cobra`).
- [x] **Expiración de presupuestos (D12):** nueva config `Empresa.diasExpiracionPresupuesto` (default `0` = sin vencimiento) en `GET/PATCH /company-settings` + `UpdateSettingsDto`; nuevo estado `EstadoPresupuesto.vencido` (migración `20260831000000_add_dias_expiracion_presupuesto` aplicada); marcado `vencido` perezoso al listar/detalle **y** job en segundo plano `PresupuestosExpiracionService` (`OnApplicationBootstrap` + `setInterval` 24 h, como las caducidades) vía `marcarVencidosGlobal()`; en la lista/detalle se expone `diasExpiracionPresupuesto` y `fechaVencimiento` y el listado/detalle muestran "Vence/Vencía el" (solo si aplica); al vencer el presupuesto `precioEfectivo` se recalcula al precio vigente y `precioVencido=true`; "Vender"/"Cancelar" siguen disponibles a precio vigente. **Resultado global actualizado: 89 passed, 0 failed.**
- [x] **Descancelar presupuesto:** `PATCH /presupuestos/:id/descancelar` (ADMIN/GERENTE) revierte `cancelado` → `abierto` (o `vencido` si venció mientras estaba cancelado) + auditoría `PRESUPUESTO_DESCANCELADO`; botón "Descancelar" (icono RotateCcw) en la fila de la lista y pie del modal solo para estado `cancelado`. **Resultado global actualizado: 94 passed, 0 failed.**
- [x] **Refactor paleta a tokens semánticos (style):** migrados los colores hardcodeados (`yellow-500`, `amber-500`, `red-600`, `orange-500`, `text-white`) a los tokens del sistema de diseño (`warning`, `error`, `success`, `on-error`/`on-primary`/`on-warning`) en las vistas de administración (Cajas, Categorías, Clientes, Combos, Cupones, Inventario, Lotes, ProductoDetalle, Productos, Proveedores, Usuarios), ProductoModalForm, RecepcionMercanciaModal, ConfirmDialog y FiadosView (icono "Eye" en Estado de Cuenta). Sin cambio funcional; `tsc --noEmit` limpio en frontend. *(commits `46a21ca`, `388bb2b`, `900e5f8`, `24739a6`)*

### Estandarización de Buscadores y Filtros Rápidos (12 Pestañas del Sidebar)
- [x] **Componente `BuscadorEstandar.tsx`:** Creado en `@/components/ui/BuscadorEstandar.tsx` con contenedor flotante independiente (`bg-surface rounded-2xl border border-outline/20 p-3.5 mb-6 shadow-sm`), input de búsqueda con botón de limpieza, interruptor `Switch` de elementos inactivos / soft-delete, botón `[Filtros]` con badge numérico indicador, botón `[Limpiar]` y botón `[Actualizar]` con spinner de carga.
- [x] **Integración en 12 pestañas:** Implementado en Clientes, Fiados, Devoluciones (Historial), Cajas Abiertas, Presupuestos, Categorías, Productos, Cupones, Stock por Producto (Inventario), Lotes y Caducidades, Proveedores y Usuarios.
- [x] **Filtros Rápidos Personalizados:** Implementados filtros específicos desplegables por pestaña (por estado de crédito, nivel de deuda, tipo de resolución, fechas/periodos, antigüedad de caja, tipo de cupón/vigencia, roles de usuario, categorías y tipo de venta). Verificado con `docker compose exec frontend npx tsc --noEmit` con 0 errores de sintaxis y tipos.

## Fase 4.8: Estilo, Personalización y Almacenamiento Estático de Tickets (D13)
- [x] **Configuración en BD (`configuracionTicket` Json):** Schema Prisma actualizado en `Empresa` con `configuracionTicket Json?`; defaults centralizados en `ticketConfig.ts`.
- [x] **Almacenamiento Estático y Servidor NestJS:** Habilitado `app.useStaticAssets(join(process.cwd(), 'uploads'), { prefix: '/uploads/' })` en `main.ts`.
- [x] **Aisle multitenant para imágenes:** Subcarpeta `uploads/logos/empresa_<empresaId>/` para aislar logos por empresa y evitar colisiones entre clientes.
- [x] **Manejo de Espacio en Disco (`ENOSPC`):** Captura de espacio lleno respondiendo mensaje estandarizado: *"En este momento no podemos subir el archivo debido al espacio insuficiente."* y toast informativo en frontend.
- [x] **Limpieza Automática de Archivos Huérfanos:** Implementado `CompanySettingsService.limpiarArchivosLogoNoUsados` para borrar logos antiguos no referenciados al actualizar o remover la imagen.
- [x] **Formateo Avanzado y Tooltips:** Soporte para `EstiloTexto` (Negrita, Subrayado, Alineación Izquierda/Centro/Derecha), tooltips con explicaciones claras sobre omisión de campos vacíos y botón de *"Imprimir Ticket de Prueba"* en `ConfiguracionView.tsx`.
- [x] **Modales POS (`VoucherModal` y `PresupuestoTicketModal`):** Aplicados estilos dinámicos, omisión estricta de campos vacíos (`!val?.trim()`) y soporte para ticket impreso y digital.

## Fase 4.9: Solicitud de Productos a Proveedores y Requisiciones (D14)
- [x] **Modelos Prisma (`SolicitudProveedor` y `SolicitudProveedorDetalle`):** Enum `EstadoSolicitudProveedor` (`BORRADOR`, `ENVIADA`, `RECIBIDA`, `CANCELADA`), relaciones con `Empresa`, `Proveedor?` (opcional: null para solicitudes abiertas / requisiciones internas), `Usuario` (creador) y `Producto`. Folio atómico por empresa `SOL-000001` mediante `secuenciaSolicitudProveedor`.
- [x] **Módulo Backend NestJS `solicitudes-proveedor`:** CRUD completo (`POST`, `GET`, `GET :id`, `PATCH :id`, `DELETE :id`), filtros por proveedor, estado, búsqueda por folio/notas, paginación estandarizada y transacciones atómicas.
- [x] **Auditoría e Integración de Eventos (`LogActividad`):** Registro automatizado de eventos auditales `SOLICITUD_PROVEEDOR_CREADA`, `SOLICITUD_PROVEEDOR_ACTUALIZADA`, `SOLICITUD_PROVEEDOR_ESTADO_CAMBIADO` y `SOLICITUD_PROVEEDOR_ELIMINADA` vía `AuditService`.
- [x] **Pantalla de Auditoría ([`AuditoriaView.tsx`](file:///c:/Users/jesus/OneDrive/Desktop/CUDII_POS/frontend/src/views/admin/AuditoriaView.tsx)):** Mapeo de badges, íconos y formateador de resúmenes `resumirDetalles` para visualizar movimientos de solicitudes de proveedores por usuario y fecha.
- [x] **Frontend React (Vista, Modales e Impresión Formal):**
  - Vista principal [`SolicitudesProveedorView.tsx`](file:///c:/Users/jesus/OneDrive/Desktop/CUDII_POS/frontend/src/views/admin/SolicitudesProveedorView.tsx) con `BuscadorEstandar`, 4 tarjetas Bento de métricas y tabla interactiva.
  - Modal de creación/edición [`NuevaSolicitudProveedorModal.tsx`](file:///c:/Users/jesus/OneDrive/Desktop/CUDII_POS/frontend/src/components/admin/NuevaSolicitudProveedorModal.tsx) con selector de proveedor u opción "Solicitud Abierta", selector dinámico de catálogo y cálculo en vivo de totales.
  - Comprobante formal e imprimible [`SolicitudProveedorPrintModal.tsx`](file:///c:/Users/jesus/OneDrive/Desktop/CUDII_POS/frontend/src/components/admin/SolicitudProveedorPrintModal.tsx) adaptado para orden de compra corporativa, botón de impresión limpia `window.print()` y generador de resumen para WhatsApp/Email.
- [x] **Integración con Navegación, Buscador y Permisos:** Registrado en `App.tsx` (`/admin/solicitudes-proveedor`), barra lateral `MainLayout.tsx` (sección Control de Inventario), buscador global `GlobalSearch.tsx` (`Ctrl + K`) y matriz de permisos `permisos.ts`.


